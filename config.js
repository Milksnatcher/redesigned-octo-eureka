// Paste your Supabase project's values here (Project Settings → API).
// The anon/public key is meant to be public — it's safe to commit and
// safe to ship in a GitHub Pages site. Do NOT use the "service_role" key here.
export const SUPABASE_URL = "YOUR_SUPABASE_PROJECT_URL";
export const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
